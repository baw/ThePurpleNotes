<% if (shareURL) { %>
  Share URL: <a id="shareUrl" href="/sharings/<%= shareURL %>" target="_blank">
    /sharings/<%= shareURL %>
  </a>
  <button class="unShare btn btn-danger">Make Private</button>
<% } else { %>
  <button class="share btn btn-default">Create Share Url</button>
<% } %>